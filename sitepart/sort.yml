Сортировка записей по полю
---
parameters:
  - name: field
    in: path
    type: string
    enum: ['id', 'country', 'language', 'speakers', 'letters', 'words']
    required: true
responses:
  '200':
    description: Отсортировано по полю
    content:
      application/json:
        schema: OutputSchema
  '500':
    description: Поле с таким именем отсутствует
